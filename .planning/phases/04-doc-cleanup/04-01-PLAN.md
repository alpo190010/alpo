---
phase: 04-doc-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MARKETING.md
  - DASHBOARD.md
  - webapp/public/status.json
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - "MARKETING.md no longer exists in the repository working tree"
    - "DASHBOARD.md no longer exists in the repository working tree"
    - "webapp/public/status.json no longer exists in the repository working tree"
    - "All three deletions are staged in git (tracked removal, not untracked)"
  artifacts:
    - path: "MARKETING.md"
      provides: "Removed — old PageScore marketing templates"
      state: "deleted"
    - path: "DASHBOARD.md"
      provides: "Removed — old founder dashboard with credential exposure"
      state: "deleted"
    - path: "webapp/public/status.json"
      provides: "Removed — stale manual tracking artifact from public dir"
      state: "deleted"
  key_links: []
---

<objective>
Delete three stale documentation files that reference old PageScore branding, $7 pricing, and contain security-sensitive credentials.

Purpose: Remove files that contradict current product positioning and eliminate a credential-exposure risk (DASHBOARD.md contains plaintext passwords in git working tree). Per CONTEXT.md decisions D-04, D-05, D-06, D-07 — all three are full deletions, not updates.

Output: Three files removed from working tree via `git rm`. Credential rotation is flagged as a deferred follow-up (D-06) — NOT in scope here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doc-cleanup/04-CONTEXT.md
@.planning/phases/04-doc-cleanup/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete MARKETING.md and DASHBOARD.md from repo root</name>
  <files>MARKETING.md, DASHBOARD.md</files>
  <read_first>
    - MARKETING.md (confirm it still exists and contains stale PageScore content before deleting)
    - DASHBOARD.md (confirm it still exists and contains stale PageScore content before deleting — do NOT copy or quote any credential values from this file)
  </read_first>
  <action>
Delete both files using git rm to ensure the deletions are properly staged:

```bash
git rm MARKETING.md DASHBOARD.md
```

This stages the removal in git so the commit reflects a tracked deletion.

IMPORTANT per D-06: DASHBOARD.md contains plaintext passwords for Vercel, LemonSqueezy, Reddit, HN, PostHog. Do NOT echo, log, copy, or reference any credential values from this file in any output, commit message, or summary. Simply delete it.

Per D-04: MARKETING.md is old PageScore/$7 copy-paste marketing templates. All content is stale. Delete entirely.

Per D-05: DASHBOARD.md contains plaintext account credentials (security concern), old $7 metrics, and irrelevant agent team tracking. Delete entirely.

After deletion, verify no other files in the repo reference either deleted file by name:

```bash
grep -r "MARKETING.md\|DASHBOARD.md" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.md" . 2>/dev/null | grep -v ".planning/" | grep -v "node_modules/" | grep -v ".git/"
```

If any references are found outside .planning/, remove or update those references. Research confirmed no code imports or links exist, but verify at execution time.
  </action>
  <verify>
    <automated>test ! -f MARKETING.md && test ! -f DASHBOARD.md && echo "PASS: both files deleted" || echo "FAIL: file(s) still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `ls MARKETING.md 2>/dev/null` produces no output (file does not exist)
    - `ls DASHBOARD.md 2>/dev/null` produces no output (file does not exist)
    - `git status --porcelain` shows `D  MARKETING.md` and `D  DASHBOARD.md` (staged deletions)
    - No grep matches for "MARKETING.md" or "DASHBOARD.md" in any .ts/.tsx/.js/.json/.md file outside .planning/
  </acceptance_criteria>
  <done>MARKETING.md and DASHBOARD.md are deleted from the working tree and staged in git. No other files reference them.</done>
</task>

<task type="auto">
  <name>Task 2: Delete webapp/public/status.json</name>
  <files>webapp/public/status.json</files>
  <read_first>
    - webapp/public/status.json (confirm it still exists and is the stale tracking artifact before deleting)
  </read_first>
  <action>
Delete the file using git rm:

```bash
git rm webapp/public/status.json
```

Per D-07: This file is a manual tracking artifact in the publicly served webapp directory. Nothing in the webapp source fetches it (verified by research via `grep -r "status.json" webapp/src/` returning zero matches). Delete entirely.

After deletion, verify no frontend code references this file:

```bash
grep -r "status\.json" webapp/src/ 2>/dev/null
```

If any references are found, they must be removed (research says none exist, but verify at execution time).
  </action>
  <verify>
    <automated>test ! -f webapp/public/status.json && echo "PASS: status.json deleted" || echo "FAIL: file still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `ls webapp/public/status.json 2>/dev/null` produces no output (file does not exist)
    - `git status --porcelain` shows `D  webapp/public/status.json` (staged deletion)
    - `grep -r "status\.json" webapp/src/ 2>/dev/null` returns zero matches
  </acceptance_criteria>
  <done>webapp/public/status.json is deleted from the working tree and staged in git. No webapp source code references it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history -> working tree | DASHBOARD.md credentials exist in git history even after working-tree deletion |
| Public web dir -> browser | webapp/public/ files are served to any visitor; status.json removal closes this surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | DASHBOARD.md | mitigate (partial) | Delete file from working tree (this plan). Full git-history rewrite deferred per D-06 — credentials remain in past commits. Credential rotation flagged as follow-up. |
| T-04-02 | Information Disclosure | webapp/public/status.json | mitigate | Delete from public dir — removes stale product data from being served to browsers. |
| T-04-03 | Information Disclosure | Commit messages / logs | mitigate | Commit message for DASHBOARD.md deletion must NOT quote or reference any credential values. Use generic "delete stale founder dashboard (security cleanup)". |
</threat_model>

<verification>
Run all three checks:

```bash
test ! -f MARKETING.md && test ! -f DASHBOARD.md && test ! -f webapp/public/status.json && echo "ALL FILES DELETED" || echo "DELETION INCOMPLETE"
```

```bash
grep -ri "pagescore" MARKETING.md DASHBOARD.md webapp/public/status.json 2>/dev/null | wc -l
# Expected: 0 (files don't exist)
```
</verification>

<success_criteria>
- MARKETING.md does not exist in working tree
- DASHBOARD.md does not exist in working tree
- webapp/public/status.json does not exist in working tree
- All three deletions are staged via git rm
- No code references to the deleted files remain outside .planning/
- No credential values from DASHBOARD.md appear in any output or commit message
</success_criteria>

<output>
After completion, create `.planning/phases/04-doc-cleanup/04-01-SUMMARY.md`
</output>
