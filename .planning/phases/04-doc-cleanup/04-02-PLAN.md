---
phase: 04-doc-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.local.example
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "README.md describes alpo.ai as a Shopify product page analyzer"
    - "README.md mentions free tier with 18-dimension scoring and revenue leak estimates"
    - "README.md contains no references to PageScore, $7, $79, OpenAI (as a service), LemonSqueezy, or Resend"
    - "README.md lists the actual tech stack: Next.js 16, React 19, FastAPI, PostgreSQL, Playwright"
    - "README.md includes a dev setup section with environment variables and startup commands"
    - ".env.local.example has updated comments that do not describe OpenAI, LemonSqueezy, or Resend as active services"
  artifacts:
    - path: "README.md"
      provides: "Product overview and dev setup guide for alpo.ai"
      min_lines: 60
      contains: "alpo.ai"
    - path: ".env.local.example"
      provides: "Env var template with corrected service descriptions"
      contains: "dormant"
  key_links:
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "Content sourced from PROJECT.md product description"
      pattern: "Shopify product page analyzer"
---

<objective>
Rewrite README.md as a product overview and dev setup guide for alpo.ai, and update .env.local.example comments to remove stale service descriptions.

Purpose: README.md currently says "PageScore -- AI Landing Page Analyzer" (one line, fully stale). Per D-01 it must become a proper product overview + dev setup guide. Per D-02 it must describe only the current product (no Pro waitlist, no business model, no future plans). Per D-03 the tech stack must reflect reality. The .env.local.example has misleading comments describing OpenAI, LemonSqueezy, and Resend as active services when they are dormant.

Output: A complete README.md and updated .env.local.example comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doc-cleanup/04-CONTEXT.md
@.planning/phases/04-doc-cleanup/04-RESEARCH.md
@.planning/phases/04-doc-cleanup/04-PATTERNS.md

<interfaces>
<!-- No code interfaces — this plan modifies documentation and config comments only. -->
<!-- Content sources are documented inline in task actions below. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md as product overview + dev setup guide</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — single line "PageScore -- AI Landing Page Analyzer")
    - .planning/PROJECT.md (source of truth for product description, tech stack, constraints)
    - .planning/phases/04-doc-cleanup/04-PATTERNS.md (section structure, table formats, startup commands, env var table — all verified from codebase)
    - .planning/phases/04-doc-cleanup/04-RESEARCH.md (verified env var inventory, tech stack table, product description, pitfalls to avoid)
    - docs/cloudflare-setup.md (structural analog — follow same markdown conventions: numbered sections, tables, fenced code blocks with language tags, blockquote callouts)
    - webapp/package.json (verify startup commands: look for "dev" script, confirm port 3005)
    - api/Dockerfile (verify backend startup command: look for CMD with uvicorn)
  </read_first>
  <action>
Replace the entire contents of README.md with the following structure. All content values below are verified from the codebase — use them verbatim, do not re-derive or guess.

**Hard constraints (from CONTEXT.md):**
- Per D-01: Product overview + dev setup guide. Not minimal, not just setup.
- Per D-02: Current product ONLY. No mention of Pro waitlist, business model, or future plans.
- Per D-03: Actual tech stack only. Remove OpenAI, LemonSqueezy, Resend references.

**Section-by-section content:**

### Header
```
# alpo.ai -- Shopify Product Page Analyzer
```

### Product description (2-3 sentences, from PROJECT.md verbatim):
"AI-powered Shopify product page analyzer that scores 18 conversion dimensions and estimates revenue leaks. Merchants paste a product URL, get a score out of 100, and see prioritized recommendations to improve their page. Free to use, targeting small solo Shopify merchants."

### What It Does section (bullet list):
- Paste any Shopify product URL
- Get a score out of 100 across 18 conversion dimensions
- See revenue leak estimates per dimension
- Get prioritized recommendations to improve your page

### Tech Stack section (table format from PATTERNS.md):

| Layer | Technology |
|-------|-----------|
| Frontend | Next.js 16 + React 19 (Vercel) |
| Backend API | FastAPI + Python |
| Database | PostgreSQL |
| Page rendering | Playwright (headless browser) |
| Auth | NextAuth.js (email/password + Google OAuth) |
| Analytics | PostHog |
| Reverse proxy | Caddy |
| Containerization | Docker Compose |

### Project Structure section:

```
alpo/
├── webapp/          # Next.js 16 + React 19 frontend
├── api/             # FastAPI Python backend
├── docs/            # Infrastructure setup guides
├── scripts/         # Provision and verify scripts
├── docker-compose.yml          # Local dev (all services)
├── docker-compose.prod.yml     # Production Docker Compose
├── Caddyfile        # Reverse proxy config
└── .env             # Environment variables (copy from .env.production.template)
```

### Getting Started section with subsections:

**Prerequisites:**
- Node.js (for webapp)
- Python 3.11+ (for API)
- Docker and Docker Compose
- PostgreSQL (or use Docker Compose)

**Environment Variables** — render this table (required vars only):

| Variable | Required | Purpose |
|----------|----------|---------|
| `POSTGRES_USER` | Yes | PostgreSQL username |
| `POSTGRES_PASSWORD` | Yes | PostgreSQL password |
| `POSTGRES_DB` | Yes | PostgreSQL database name |
| `DATABASE_URL` | Yes | Full PostgreSQL connection string |
| `AUTH_SECRET` | Yes | NextAuth.js signing secret |
| `AUTH_GOOGLE_ID` | Yes | Google OAuth client ID |
| `AUTH_GOOGLE_SECRET` | Yes | Google OAuth client secret |
| `NEXT_PUBLIC_API_URL` | Yes | URL the webapp uses to reach the FastAPI backend |
| `NEXT_PUBLIC_BASE_URL` | Yes | Public base URL for the webapp |

After the table, add a blockquote callout for dormant vars:

> **Dormant (not required to run core product):** `OPENAI_API_KEY` (OpenRouter key for optional AI features), `RESEND_API_KEY` (email, unused at launch), `LEMONSQUEEZY_WEBHOOK_SECRET` and `LEMONSQUEEZY_VARIANT_*` (payments, no paid tier yet).

**Installation:**
```bash
git clone <repo-url>
cp .env.production.template .env
# Edit .env with your values
```

### Development section:

**Running Locally** — use these verified commands:

```bash
# Frontend (webapp/)
cd webapp && npm install && npm run dev    # starts Next.js on port 3005

# Backend (api/) -- start Postgres first
docker compose -f docker-compose.dev.yml up db
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Full stack (all services via Docker Compose)
docker compose up
```

**Formatting conventions to follow** (from docs/cloudflare-setup.md analog):
- Use `---` horizontal rules between major sections
- Use fenced code blocks with language tags (```bash, ```yaml, etc.) — never untagged
- Use markdown tables with header separator rows
- Use `> **Note:**` blockquote pattern for callouts
- Direct, factual tone — no marketing fluff

**Content the README must NOT contain** (per D-02):
- Any mention of "Pro", "waitlist", "paid tier", or "business model"
- Any mention of "future plans", "roadmap", or "coming soon"
- OpenAI described as an active service
- LemonSqueezy described as an active service
- Resend described as an active service
- "$7", "$79", "one-time", or "PageScore"
  </action>
  <verify>
    <automated>grep -ci "pagescore\|\\$7\|\\$79\|one-time report" README.md 2>/dev/null; test $? -ne 0 && echo "PASS: no stale refs" || echo "FAIL: stale references found"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 README.md` outputs `# alpo.ai` (starts with product name, not PageScore)
    - `grep -c "alpo.ai" README.md` returns at least 1 (product name present)
    - `grep -c "Shopify" README.md` returns at least 1 (product description present)
    - `grep -c "18 conversion dimensions\|18 dimensions" README.md` returns at least 1 (feature described)
    - `grep -c "Next.js 16" README.md` returns at least 1 (tech stack present)
    - `grep -c "FastAPI" README.md` returns at least 1 (tech stack present)
    - `grep -c "PostgreSQL" README.md` returns at least 1 (tech stack present)
    - `grep -c "Playwright" README.md` returns at least 1 (tech stack present)
    - `grep -c "POSTGRES_USER\|POSTGRES_PASSWORD\|DATABASE_URL\|AUTH_SECRET" README.md` returns at least 4 (env vars documented)
    - `grep -c "npm run dev" README.md` returns at least 1 (startup command present)
    - `grep -c "docker compose" README.md` returns at least 1 (docker startup present)
    - `grep -ci "pagescore" README.md` returns 0 (no stale branding)
    - `grep -ci "\\$7\|\\$79" README.md` returns 0 (no stale pricing)
    - `grep -ci "pro waitlist\|business model\|future plan\|coming soon" README.md` returns 0 (per D-02, no future plans)
    - `wc -l < README.md` returns at least 60 (substantive content, not a stub)
  </acceptance_criteria>
  <done>README.md is a complete product overview and dev setup guide for alpo.ai. Contains product description, tech stack table, project structure, env var table, and startup commands. No references to PageScore, old pricing, or future plans.</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.local.example comments to remove stale service descriptions</name>
  <files>.env.local.example</files>
  <read_first>
    - .env.local.example (current state with stale comments)
    - .env.production.template (analog for correct comment style: `# --- Section header ---` pattern)
    - .planning/phases/04-doc-cleanup/04-PATTERNS.md (correct replacement pattern documented in "`.env.local.example` (config template)" section)
  </read_first>
  <action>
Replace the entire contents of .env.local.example with corrected comments. Keep all the same variable names and placeholder values — only the comments change.

**Current stale content** (replace this entirely):
```bash
# OpenAI - powers the AI analysis          <-- WRONG: hits OpenRouter, not OpenAI
OPENAI_API_KEY=sk-...

# Lemon Squeezy - payment processing       <-- WRONG: dormant, no paid tier
NEXT_PUBLIC_LEMONSQUEEZY_CHECKOUT_URL=https://yourstore.lemonsqueezy.com/buy/xxx
LEMONSQUEEZY_WEBHOOK_SECRET=your-webhook-secret

# Resend - email delivery for paid reports  <-- WRONG: dormant at launch
RESEND_API_KEY=re_...

# API URL - points the webapp at the FastAPI backend  <-- OK
NEXT_PUBLIC_API_URL=http://localhost:8000

# CORS - allowed origins for the FastAPI backend      <-- OK
CORS_ORIGINS=http://localhost:3000
```

**Replace with** (following .env.production.template `# --- Section ---` comment style):
```bash
# --- OpenRouter (optional AI features -- env var named OPENAI_API_KEY but hits openrouter.ai, dormant at launch) ---
OPENAI_API_KEY=sk-...

# --- LemonSqueezy (payment webhooks -- dormant, no paid tier at launch) ---
NEXT_PUBLIC_LEMONSQUEEZY_CHECKOUT_URL=https://yourstore.lemonsqueezy.com/buy/xxx
LEMONSQUEEZY_WEBHOOK_SECRET=your-webhook-secret

# --- Resend (email delivery -- dormant at launch) ---
RESEND_API_KEY=re_...

# --- API URL -- points the webapp at the FastAPI backend ---
NEXT_PUBLIC_API_URL=http://localhost:8000

# --- CORS -- allowed origins for the FastAPI backend ---
CORS_ORIGINS=http://localhost:3000
```

Key changes:
1. "OpenAI - powers the AI analysis" -> "OpenRouter (optional AI features -- env var named OPENAI_API_KEY but hits openrouter.ai, dormant at launch)"
2. "Lemon Squeezy - payment processing" -> "LemonSqueezy (payment webhooks -- dormant, no paid tier at launch)"
3. "Resend - email delivery for paid reports" -> "Resend (email delivery -- dormant at launch)"
4. All comments now use `# --- Section ---` format matching .env.production.template
  </action>
  <verify>
    <automated>grep -c "dormant" .env.local.example; test $? -eq 0 && echo "PASS: dormant markers present" || echo "FAIL: missing dormant markers"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "OpenRouter" .env.local.example` returns a match (OpenAI replaced with OpenRouter description)
    - `grep "dormant" .env.local.example` returns at least 3 matches (all three stale services marked dormant)
    - `grep -c "powers the AI analysis" .env.local.example` returns 0 (stale OpenAI description removed)
    - `grep -c "payment processing" .env.local.example` returns 0 (stale LemonSqueezy description removed)
    - `grep -c "email delivery for paid reports" .env.local.example` returns 0 (stale Resend description removed)
    - `grep "NEXT_PUBLIC_API_URL" .env.local.example` returns a match (non-stale vars preserved)
    - `grep "CORS_ORIGINS" .env.local.example` returns a match (non-stale vars preserved)
    - `grep "OPENAI_API_KEY=sk-" .env.local.example` returns a match (placeholder value preserved)
    - `grep "# ---" .env.local.example` returns at least 5 matches (consistent comment style)
  </acceptance_criteria>
  <done>.env.local.example has updated comments: OpenAI described as OpenRouter, all three dormant services marked "dormant at launch", comment style matches .env.production.template convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md -> public GitHub | README content is visible to anyone viewing the repo |
| .env.local.example -> developers | Template guides developers on what services to configure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | README.md | mitigate | Do not include actual API keys, secrets, or internal URLs in README. Only placeholder patterns and variable names. Env var table shows names and purposes, not values. |
| T-04-05 | Information Disclosure | .env.local.example | mitigate | Keep only placeholder values (sk-..., re_..., your-webhook-secret). Never include real keys. Already the current pattern — preserve it. |
| T-04-06 | Spoofing | README.md | accept | README could be edited by anyone with write access. Standard git access control applies. Low risk — documentation file. |
</threat_model>

<verification>
Comprehensive stale-content check across both files:

```bash
grep -ci "pagescore\|\\$7\|\\$79\|one-time report\|AI Landing Page Analyzer" README.md .env.local.example 2>/dev/null
# Expected: 0 matches across both files
```

```bash
grep -c "powers the AI analysis\|payment processing\|email delivery for paid reports" .env.local.example 2>/dev/null
# Expected: 0 (all stale descriptions replaced)
```

```bash
wc -l < README.md
# Expected: >= 60 lines (substantive content)
```
</verification>

<success_criteria>
- README.md is a complete product overview + dev setup guide (>= 60 lines)
- README.md uses "alpo.ai" as the product name, not "PageScore"
- README.md documents the actual tech stack (Next.js 16, React 19, FastAPI, PostgreSQL, Playwright)
- README.md includes env var table with required variables
- README.md includes dev startup commands (npm run dev, docker compose up)
- README.md contains zero references to PageScore, $7, $79, Pro waitlist, or future plans
- .env.local.example has corrected comments marking OpenAI/LemonSqueezy/Resend as dormant
- .env.local.example preserves all variable names and placeholder values
</success_criteria>

<output>
After completion, create `.planning/phases/04-doc-cleanup/04-02-SUMMARY.md`
</output>
